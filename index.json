{
  "api/Fl.Event.Handling.ExceptionCatcherEventProcessor-1.html": {
    "href": "api/Fl.Event.Handling.ExceptionCatcherEventProcessor-1.html",
    "title": "Class ExceptionCatcherEventProcessor<TEvent> | Functional Redis Documentation",
    "summary": "Class ExceptionCatcherEventProcessor<TEvent> Namespace Fl.Event.Handling Assembly Fl.Event.Handling.dll An IEventProcessor<TEvent> decorator that catches any unhandled exceptions thrown by the inner processor and converts them into LanguageExt.Common.Error values. public class ExceptionCatcherEventProcessor<TEvent> : IEventProcessor<TEvent> where TEvent : class Type Parameters TEvent The type of the event to process. Must be a reference type. Inheritance object ExceptionCatcherEventProcessor<TEvent> Implements IEventProcessor<TEvent> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Constructors ExceptionCatcherEventProcessor(IEventProcessor<TEvent>) An IEventProcessor<TEvent> decorator that catches any unhandled exceptions thrown by the inner processor and converts them into LanguageExt.Common.Error values. public ExceptionCatcherEventProcessor(IEventProcessor<TEvent> processor) Parameters processor IEventProcessor<TEvent> The inner IEventProcessor<TEvent> to wrap. Methods ProcessAsync(TEvent) Asynchronously processes the specified event, catching any exceptions thrown by the inner processor and returning them as LanguageExt.Common.Error values. public EitherAsync<Error, Unit> ProcessAsync(TEvent evt) Parameters evt TEvent The event instance to process. Returns EitherAsync<Error, Unit> An LanguageExt.EitherAsync<L, R> that resolves to LanguageExt.Unit on success, or an LanguageExt.Common.Error wrapping the caught exception if processing fails."
  },
  "api/Fl.Event.Handling.Extensions.EventHandlingStrategy.html": {
    "href": "api/Fl.Event.Handling.Extensions.EventHandlingStrategy.html",
    "title": "Enum EventHandlingStrategy | Functional Redis Documentation",
    "summary": "Enum EventHandlingStrategy Namespace Fl.Event.Handling.Extensions Assembly Fl.Event.Handling.Extensions.dll Specifies the strategy used to dispatch events to their registered handlers. public enum EventHandlingStrategy Fields Parallel = 1 Handlers are invoked concurrently and all are awaited before the result is returned. Sequential = 0 Handlers are invoked one at a time, in registration order. Processing stops immediately if any handler returns an error."
  },
  "api/Fl.Event.Handling.Extensions.ServiceCollectionExtensions.html": {
    "href": "api/Fl.Event.Handling.Extensions.ServiceCollectionExtensions.html",
    "title": "Class ServiceCollectionExtensions | Functional Redis Documentation",
    "summary": "Class ServiceCollectionExtensions Namespace Fl.Event.Handling.Extensions Assembly Fl.Event.Handling.Extensions.dll Provides extension methods for registering event handling services with the IServiceCollection dependency injection container. public static class ServiceCollectionExtensions Inheritance object ServiceCollectionExtensions Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Methods AddEventHandling<TEvent>(IServiceCollection, EventHandlingStrategy, Assembly) Registers an IEventProcessor<TEvent> and all IEventHandler<TEvent> implementations found in the specified assembly with the DI container. public static IServiceCollection AddEventHandling<TEvent>(this IServiceCollection services, EventHandlingStrategy strategy, Assembly assembly) where TEvent : class Parameters services IServiceCollection The IServiceCollection to add the services to. strategy EventHandlingStrategy The EventHandlingStrategy that determines whether handlers are invoked sequentially or in parallel. assembly Assembly The Assembly to scan for IEventHandler<TEvent> implementations. Returns IServiceCollection The original services instance for method chaining. Type Parameters TEvent The event type to configure handling for. Must be a reference type. Exceptions ArgumentException Thrown when strategy is not a recognised EventHandlingStrategy value."
  },
  "api/Fl.Event.Handling.Extensions.html": {
    "href": "api/Fl.Event.Handling.Extensions.html",
    "title": "Namespace Fl.Event.Handling.Extensions | Functional Redis Documentation",
    "summary": "Namespace Fl.Event.Handling.Extensions Classes ServiceCollectionExtensions Provides extension methods for registering event handling services with the IServiceCollection dependency injection container. Enums EventHandlingStrategy Specifies the strategy used to dispatch events to their registered handlers."
  },
  "api/Fl.Event.Handling.IEventHandler-1.html": {
    "href": "api/Fl.Event.Handling.IEventHandler-1.html",
    "title": "Interface IEventHandler<TEvent> | Functional Redis Documentation",
    "summary": "Interface IEventHandler<TEvent> Namespace Fl.Event.Handling Assembly Fl.Event.Handling.dll Defines a handler responsible for processing a single event of type TEvent. public interface IEventHandler<TEvent> where TEvent : class Type Parameters TEvent The type of the event to handle. Must be a reference type. Methods HandleAsync(TEvent) Asynchronously handles the specified event. EitherAsync<Error, Unit> HandleAsync(TEvent evt) Parameters evt TEvent The event instance to handle. Returns EitherAsync<Error, Unit> An LanguageExt.EitherAsync<L, R> that resolves to LanguageExt.Unit on success, or an LanguageExt.Common.Error if handling fails."
  },
  "api/Fl.Event.Handling.IEventProcessor-1.html": {
    "href": "api/Fl.Event.Handling.IEventProcessor-1.html",
    "title": "Interface IEventProcessor<TEvent> | Functional Redis Documentation",
    "summary": "Interface IEventProcessor<TEvent> Namespace Fl.Event.Handling Assembly Fl.Event.Handling.dll Defines a processor that orchestrates the handling of events of type TEvent. public interface IEventProcessor<TEvent> where TEvent : class Type Parameters TEvent The type of the event to process. Must be a reference type. Methods ProcessAsync(TEvent) Asynchronously processes the specified event. EitherAsync<Error, Unit> ProcessAsync(TEvent evt) Parameters evt TEvent The event instance to process. Returns EitherAsync<Error, Unit> An LanguageExt.EitherAsync<L, R> that resolves to LanguageExt.Unit on success, or an LanguageExt.Common.Error if processing fails."
  },
  "api/Fl.Event.Handling.LoggingEventProcessor-1.html": {
    "href": "api/Fl.Event.Handling.LoggingEventProcessor-1.html",
    "title": "Class LoggingEventProcessor<TEvent> | Functional Redis Documentation",
    "summary": "Class LoggingEventProcessor<TEvent> Namespace Fl.Event.Handling Assembly Fl.Event.Handling.dll An IEventProcessor<TEvent> decorator that adds structured logging around the inner processor's execution using a Serilog Serilog.ILogger. public class LoggingEventProcessor<TEvent> : IEventProcessor<TEvent> where TEvent : class Type Parameters TEvent The type of the event to process. Must be a reference type. Inheritance object LoggingEventProcessor<TEvent> Implements IEventProcessor<TEvent> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Constructors LoggingEventProcessor(IEventProcessor<TEvent>, ILogger) An IEventProcessor<TEvent> decorator that adds structured logging around the inner processor's execution using a Serilog Serilog.ILogger. public LoggingEventProcessor(IEventProcessor<TEvent> processor, ILogger logger) Parameters processor IEventProcessor<TEvent> The inner IEventProcessor<TEvent> to wrap. logger ILogger The Serilog logger used to record processing outcomes. Methods ProcessAsync(TEvent) Asynchronously processes the specified event and logs the outcome via the configured Serilog.ILogger. public EitherAsync<Error, Unit> ProcessAsync(TEvent evt) Parameters evt TEvent The event instance to process. Returns EitherAsync<Error, Unit> An LanguageExt.EitherAsync<L, R> that resolves to LanguageExt.Unit on success, or an LanguageExt.Common.Error if processing fails."
  },
  "api/Fl.Event.Handling.ParallelEventProcessor-1.html": {
    "href": "api/Fl.Event.Handling.ParallelEventProcessor-1.html",
    "title": "Class ParallelEventProcessor<T> | Functional Redis Documentation",
    "summary": "Class ParallelEventProcessor<T> Namespace Fl.Event.Handling Assembly Fl.Event.Handling.dll An IEventProcessor<TEvent> that dispatches an event to all registered IEventHandler<TEvent> instances concurrently. public class ParallelEventProcessor<T> : IEventProcessor<T> where T : class Type Parameters T The type of the event to process. Must be a reference type. Inheritance object ParallelEventProcessor<T> Implements IEventProcessor<T> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Constructors ParallelEventProcessor(IEnumerable<IEventHandler<T>>) An IEventProcessor<TEvent> that dispatches an event to all registered IEventHandler<TEvent> instances concurrently. public ParallelEventProcessor(IEnumerable<IEventHandler<T>> eventHandlers) Parameters eventHandlers IEnumerable<IEventHandler<T>> The collection of handlers to invoke in parallel. Methods ProcessAsync(T) Asynchronously dispatches the specified event to all registered handlers in parallel, awaiting all of them before returning. public EitherAsync<Error, Unit> ProcessAsync(T evt) Parameters evt T The event instance to process. Returns EitherAsync<Error, Unit> An LanguageExt.EitherAsync<L, R> that resolves to LanguageExt.Unit when all handlers succeed, or an LanguageExt.Common.Error if any handler fails."
  },
  "api/Fl.Event.Handling.SequentialEventProcessor-1.html": {
    "href": "api/Fl.Event.Handling.SequentialEventProcessor-1.html",
    "title": "Class SequentialEventProcessor<T> | Functional Redis Documentation",
    "summary": "Class SequentialEventProcessor<T> Namespace Fl.Event.Handling Assembly Fl.Event.Handling.dll An IEventProcessor<TEvent> that dispatches an event to all registered IEventHandler<TEvent> instances one at a time, in order. public class SequentialEventProcessor<T> : IEventProcessor<T> where T : class Type Parameters T The type of the event to process. Must be a reference type. Inheritance object SequentialEventProcessor<T> Implements IEventProcessor<T> Inherited Members object.Equals(object) object.Equals(object, object) object.GetHashCode() object.GetType() object.MemberwiseClone() object.ReferenceEquals(object, object) object.ToString() Constructors SequentialEventProcessor(IEnumerable<IEventHandler<T>>) An IEventProcessor<TEvent> that dispatches an event to all registered IEventHandler<TEvent> instances one at a time, in order. public SequentialEventProcessor(IEnumerable<IEventHandler<T>> eventHandlers) Parameters eventHandlers IEnumerable<IEventHandler<T>> The ordered collection of handlers to invoke sequentially. Methods ProcessAsync(T) Asynchronously dispatches the specified event to all registered handlers one at a time, stopping early if any handler returns an LanguageExt.Common.Error. public EitherAsync<Error, Unit> ProcessAsync(T evt) Parameters evt T The event instance to process. Returns EitherAsync<Error, Unit> An LanguageExt.EitherAsync<L, R> that resolves to LanguageExt.Unit when all handlers succeed, or an LanguageExt.Common.Error if any handler fails."
  },
  "api/Fl.Event.Handling.html": {
    "href": "api/Fl.Event.Handling.html",
    "title": "Namespace Fl.Event.Handling | Functional Redis Documentation",
    "summary": "Namespace Fl.Event.Handling Classes ExceptionCatcherEventProcessor<TEvent> An IEventProcessor<TEvent> decorator that catches any unhandled exceptions thrown by the inner processor and converts them into LanguageExt.Common.Error values. LoggingEventProcessor<TEvent> An IEventProcessor<TEvent> decorator that adds structured logging around the inner processor's execution using a Serilog Serilog.ILogger. ParallelEventProcessor<T> An IEventProcessor<TEvent> that dispatches an event to all registered IEventHandler<TEvent> instances concurrently. SequentialEventProcessor<T> An IEventProcessor<TEvent> that dispatches an event to all registered IEventHandler<TEvent> instances one at a time, in order. Interfaces IEventHandler<TEvent> Defines a handler responsible for processing a single event of type TEvent. IEventProcessor<TEvent> Defines a processor that orchestrates the handling of events of type TEvent."
  },
  "docs/fl-event-handling-extensions.html": {
    "href": "docs/fl-event-handling-extensions.html",
    "title": "Fl.Event.Handling.Extensions | Functional Redis Documentation",
    "summary": "Fl.Event.Handling.Extensions This library provides dependency injection extensions for registering the event handling components from Fl.Event.Handling into an IServiceCollection. Components EventHandlingStrategy An enum that controls how an event is dispatched to its registered handlers. Value Description Sequential Handlers are invoked one at a time, in registration order. Processing stops immediately if any handler returns an error. Parallel Handlers are invoked concurrently and all are awaited before the result is returned. ServiceCollectionExtensions Provides the AddEventHandling<TEvent> extension method on IServiceCollection. IServiceCollection AddEventHandling<TEvent>( this IServiceCollection services, EventHandlingStrategy strategy, Assembly assembly) where TEvent : class This method: Registers an IEventProcessor<TEvent> singleton — either SequentialEventProcessor<TEvent> or ParallelEventProcessor<TEvent> — depending on the chosen strategy. Scans the provided assembly and registers all concrete IEventHandler<TEvent> implementations as singleton services. An ArgumentException is thrown if strategy is not a valid EventHandlingStrategy value. Usage using Fl.Event.Handling.Extensions; using System.Reflection; // Inside your IServiceCollection configuration (e.g. Program.cs or a startup class): services.AddEventHandling<MyEvent>( EventHandlingStrategy.Sequential, Assembly.GetExecutingAssembly()); To use the parallel strategy instead: services.AddEventHandling<MyEvent>( EventHandlingStrategy.Parallel, Assembly.GetExecutingAssembly()); Multiple event types can be registered independently: services.AddEventHandling<OrderPlacedEvent>(EventHandlingStrategy.Sequential, Assembly.GetExecutingAssembly()); services.AddEventHandling<PaymentReceivedEvent>(EventHandlingStrategy.Parallel, Assembly.GetExecutingAssembly()); Each call scans the assembly for the corresponding IEventHandler<TEvent> implementations and registers them alongside their processor."
  },
  "docs/fl-event-handling.html": {
    "href": "docs/fl-event-handling.html",
    "title": "Fl.Event.Handling | Functional Redis Documentation",
    "summary": "Fl.Event.Handling This library provides the core abstractions and implementations for event handling using a functional approach built on top of LanguageExt. Abstractions IEventHandler<TEvent> Represents a single handler responsible for reacting to an event of type TEvent. public interface IEventHandler<TEvent> where TEvent : class { EitherAsync<Error, Unit> HandleAsync(TEvent evt); } Implement this interface to define the logic that should execute when a specific event is received. Multiple implementations can be registered for the same TEvent and composed through an IEventProcessor<TEvent>. IEventProcessor<TEvent> Represents an orchestrator that coordinates one or more IEventHandler<TEvent> instances to process an event. public interface IEventProcessor<TEvent> where TEvent : class { EitherAsync<Error, Unit> ProcessAsync(TEvent evt); } Both methods return EitherAsync<Error, Unit>: an asynchronous computation that yields Unit on success or an Error on failure, keeping the processing pipeline fully functional and free of thrown exceptions. Processors SequentialEventProcessor<T> Dispatches an event to all registered IEventHandler<T> instances one at a time, in registration order. If any handler returns an Error, processing stops immediately and the error is propagated. var processor = new SequentialEventProcessor<MyEvent>(handlers); EitherAsync<Error, Unit> result = processor.ProcessAsync(myEvent); ParallelEventProcessor<T> Dispatches an event to all registered IEventHandler<T> instances concurrently, awaiting all of them before returning. Use this when handlers are independent and latency matters. var processor = new ParallelEventProcessor<MyEvent>(handlers); EitherAsync<Error, Unit> result = processor.ProcessAsync(myEvent); Decorators Both decorators implement IEventProcessor<TEvent> and wrap an existing processor, following the decorator pattern so they can be freely composed. ExceptionCatcherEventProcessor<TEvent> Wraps an inner processor and converts any unhandled exception thrown during processing into an Error value, preventing exceptions from escaping the functional pipeline. IEventProcessor<MyEvent> safe = new ExceptionCatcherEventProcessor<MyEvent>(innerProcessor); LoggingEventProcessor<TEvent> Wraps an inner processor and logs the outcome of each processing call using a Serilog ILogger. IEventProcessor<MyEvent> logged = new LoggingEventProcessor<MyEvent>(innerProcessor, logger); Composition example Decorators can be stacked to combine concerns: IEventProcessor<MyEvent> processor = new LoggingEventProcessor<MyEvent>( new ExceptionCatcherEventProcessor<MyEvent>( new SequentialEventProcessor<MyEvent>(handlers)), logger); This creates a processor that handles events sequentially, catches any exceptions, and logs every outcome."
  },
  "index.html": {
    "href": "index.html",
    "title": "Event Handling made simple | Functional Redis Documentation",
    "summary": "version 0.1.0 repo url: https://github.com/fabiolune/event-handling Event Handling made simple A collection of lightweight, functional C# libraries for building event-driven pipelines. All processing results are expressed as EitherAsync<Error, Unit> (via LanguageExt), keeping the pipelines composable and exception-free by design. Libraries Fl.Event.Handling The core library. Provides: IEventHandler<TEvent> — the unit of work: a single handler for a specific event type. IEventProcessor<TEvent> — the orchestrator: coordinates one or more handlers. SequentialEventProcessor<T> and ParallelEventProcessor<T> — built-in processor implementations. ExceptionCatcherEventProcessor<TEvent> and LoggingEventProcessor<TEvent> — decorators for cross-cutting concerns. Fl.Event.Handling.Extensions Dependency injection extensions for ASP.NET Core / IServiceCollection. Provides AddEventHandling<TEvent>() to register a processor and auto-discover all IEventHandler<TEvent> implementations from an assembly in a single call."
  }
}